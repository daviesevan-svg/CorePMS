# Build artifacts
/_build/
/cover/
/deps/
/doc/
/.fetch
erl_crash.dump
*.ez

# Generated
/priv/static/assets/
/priv/static/cache_manifest.json

# Uploaded photos — binaries live in object storage in prod; in dev
# we write them under priv/static/uploads/ but never commit the blobs.
/priv/static/uploads/*
!/priv/static/uploads/.gitkeep

# Node
/assets/node_modules/

# Secrets / env
/config/*.secret.exs
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.elixir_ls/
.vscode/
.idea/
*.swp
*~

# Local overrides
/config/dev.local.exs
